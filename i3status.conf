# :.config/i3status/config
# vi:syntax=conf

general {
        output_format = "i3bar"
        colors = true
        interval = 5
        color_good = "#a0a0a0"
        color_bad = "#747474"
        color_degraded = "#7c7c7c"
}

#order += "disk /"
#order += "disk /home"
#order += "disk /media"
order += "ethernet enp5s0"

disk "/home" {
    format = " %free"
    prefix_type = "custom"
    low_threshold = "20"
    threshold_type = "gbytes_avail"
}

disk "/" {
    format = " %free"
    prefix_type = "custom"
    low_threshold = "20"
    threshold_type = "gbytes_avail"
}

disk "/media" {
    format = " %free"
    prefix_type = "custom"
    low_threshold = "20"
    threshold_type = "gbytes_avail"
}

ethernet enp5s0 {
    format_up = "[IP] %ip "
    format_down = "IP n/a "
}


